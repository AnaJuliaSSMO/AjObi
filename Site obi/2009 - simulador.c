#include <stdio.h>
int main(void)
{
	int m,n,x,y;
	int fila[1000];
	char soui;

	scanf("%d %d\n",&n,&m);
	scanf("%c %d %d", &soui,&x,&y);

	for(int i = 1; i <= n; ++i)
	{
		fila[i] = i;
	}

	for(int i = 0; i < count; ++i)
	{
		if(soui == 'I')
		{
		
		}

		else
		{

		}
	}	
	return 0;	
}